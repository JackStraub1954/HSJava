This folder contains auxiliary files for support javadoc generation
for this package.

"TicTacToeDemo.h" is an html file. It is named with a .h suffix, because with
the .html suffix javadoc insists on messing with, and navigation logic
and so forth